import { ProfileAvatarPipe } from './profile-avatar.pipe';

describe('ProfileAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
