import { ProfileFIOPipe } from './profile-fio.pipe';

describe('ProfileFIOPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileFIOPipe();
    expect(pipe).toBeTruthy();
  });
});
